version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/backend
    file_exists_behavior: OVERWRITE

hooks:
  ApplicationStop:
    - location: stop.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: install.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: start.sh
      timeout: 300
      runas: root
